# DealDesk — self-hosted AI CRE deal workspace. Lean by design.
# SQLite + IMAP/SMTP are stdlib; HTMX/Tailwind are CDN (no JS build).
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
jinja2==3.1.5
itsdangerous==2.2.0          # signed session cookies (Starlette SessionMiddleware)
python-multipart==0.0.20     # form posts + file uploads from HTMX
anthropic==0.116.0           # needs messages.parse (structured outputs); AI degrades to rules without a key
pypdf==5.1.0                 # PDF term-sheet text extraction (optional; degrades if absent)
python-docx==1.1.2           # .docx text extraction (optional; degrades if absent)
openpyxl==3.1.5              # builds the .xlsx underwriting model
